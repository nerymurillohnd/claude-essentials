---
max_turns: 25
timeout_seconds: 600
allowed_tools: [Read, Glob, Grep, Skill, Bash, Write, Edit]
runs: 3
---
En `calc.py` agrega una función `average(values)` que devuelva el promedio de una lista de números (0 si la lista está vacía). No cambies el comportamiento de lo que ya existe.
