---
description: A read-only Git question must not trigger the skill.
tags: [no-trigger]
max_turns: 6
allowed_tools: [Read, Glob, Grep, Skill]
---

Explain what `git log --oneline -n 3` and `git diff HEAD~1 --stat` show. I just want to understand the output format; don't run or change anything.
