---
description: A question about a real settings key must trigger the skill and be answered from the live docs with a verbatim quote, not from memory.
tags: [trigger]
max_turns: 10
allowed_tools: [Read, Glob, Grep, Skill, Bash, WebFetch]
---

I want new worktrees to branch from my local HEAD instead of the remote default branch. Which key in `.claude/settings.json` controls that, and what values does it accept?
