---
type: llm
---

PASS if the answer names the settings key `worktree.baseRef`, reproduces the documentation's own sentence about it inside quotation marks rather than paraphrasing, and cites where that sentence came from (a `code.claude.com` URL, a section name, or the version or date checked).
FAIL if it names a different or invented key, describes the key only in its own words with no quoted sentence, or gives no source at all.
