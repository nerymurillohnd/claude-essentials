schema_version: "1.1"
name: 02-workbook-audit-no-invented-values
description: A workbook written by a program has formulas with no cached values; the audit must say so instead of presenting totals as stored values.
tags: [tabular, xlsx, provenance]
context:
  add_dirs: [resources]
execution:
  max_turns: 40
  timeout_seconds: 600
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Agent]
