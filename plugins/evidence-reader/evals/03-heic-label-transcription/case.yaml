schema_version: "1.1"
name: 03-heic-label-transcription
description: A HEIC phone photo must be converted and actually viewed; Read returns raw bytes for HEIC.
tags: [images, heic]
context:
  add_dirs: [resources]
execution:
  max_turns: 30
  timeout_seconds: 600
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Agent]
