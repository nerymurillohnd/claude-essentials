schema_version: "1.1"
name: 01-full-pdf-review
description: A 45-page PDF read in full; the last page's value is quoted exactly with a page and line receipt.
tags: [documents, pdf, coverage]
context:
  add_dirs: [resources]
execution:
  max_turns: 60
  timeout_seconds: 900
  allowed_tools: [Read, Glob, Grep, Skill, Bash, Agent]
